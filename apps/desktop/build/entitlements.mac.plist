<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Network: HTTP/HTTPS access to daemon (localhost:3100) and GitHub updates -->
	<key>com.apple.security.network.client</key>
	<true/>

	<!-- Audio: future voice interaction support -->
	<key>com.apple.security.device.audio-input</key>
	<true/>

	<!-- USB: future external device support -->
	<key>com.apple.security.device.usb</key>
	<true/>

	<!-- File access: read/write to ~/.paperclip and temp directories -->
	<key>com.apple.security.files.user-selected.read-write</key>
	<true/>

	<!-- Hardened Runtime exceptions for JIT (needed by embedded PG) -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>

	<!-- Allow unsigned executable memory (needed by PG native extensions) -->
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>

	<!-- Disable library validation (for dynamically loaded PG extensions) -->
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
